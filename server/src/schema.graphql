type User {
  username: String!
  name: String!
  password: String!
  profilePicture: String!
}

type Post {
  id: ID!
  postBody: String!
  username: String!
  name: String!
  createdAt: String!
  modifiedAt: String!
  comments: [Comment]!
  likes: [Like]!
  likesCount: Int!
  commentsCount: Int!
}

type Comment {
  id: ID!
  username: String!
  name: String!
  commentBody: String!
  createdAt: String!
  modifiedAt: String!
  likes: [Like]!
  likesCount: Int!
}

type Like {
  id: ID!
  username: String!
  createdAt: String!
}

type Query {
  getProfilePicture(username: String!): String

  getPosts: [Post]
  getPost(postId: ID!): Post
}

type Mutation {
  register(username: String!, name: String!, password: String!): String
  login(username: String!, password: String!): String

  createPost(postBody: String!): Boolean
  deletePost(postId: ID!): Boolean

  createComment(postId: ID!, commentBody: String!): Boolean
  deleteComment(postId: ID!, commentId: ID!): Boolean
  toggleLikePost(postId: ID!): Boolean
  toggleLikeComment(postId: ID!, commentId: ID!): Boolean
}
